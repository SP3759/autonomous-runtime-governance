# ARG Financial Circuit Breaker -- Python dependencies
# Python 3.11+

# Core
pydantic>=2.7.0,<3.0.0

# Kafka pipeline (optional -- simulation runs without this)
# Install for full pipeline support:
#   pip install confluent-kafka
confluent-kafka>=2.4.0; extra == "kafka"

# Development / testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
